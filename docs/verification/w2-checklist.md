# W2 live run checklist (owner-driven, 2026-07-18)

Action → expected. Wording provisional. Mark results per item; failures get
verbatim utterances if possible. Companion to w2-acceptance.md.

## Title
1. Launch → "Main menu." Then V/D/K/L → each refuses with "Main menu."
2. F1 → short title list only (arrows, Enter, Z, F1).
3. Z → repeats last utterance.
4. Tab / C / F2 / grave / brackets → silent, everywhere, all session.

## Station map
5. Load save → "On station."
6. L → "Station map, the [zone]. [marker]."
7. V → "Cycle N. Energy X of 5. Condition C, [band]. Cryo K." (values sane).
8. D → "N dice: values." + "M spent." when true.
9. K on bare map → "No clocks at this location." (by design; map clocks = W4 N tree).
10. Arrows → native marker movement, unchanged feel.
11. Enter with nothing focused → re-anchor to nearest marker, announced.
12. F1 → full station list.

## Windows
13. U → open + points; Up/Down review; Backspace or U closes; S/J inside → refusals.
14. J → "Drive log open."; Backspace close = NEW designed wiring (ledgered
    ambiguity behind it — record exact behavior); J also closes.
15. I → inventory; arrows across slots; Backspace → clean designed close.
16. Esc → "Paused." + autosave line; Backspace unpauses (designed Pause Back);
    inside Sure? → Backspace backs out one level; U while paused → refusal.

## Location and dice
17. Enter on location → action view; arrows walk actions ("2 of 4" framing = W4).
18. K at location → clock dump with progress.
19. Picker: Enter action → picker; Enter die → slots and RESTS; Backspace with
    die resting → die returns, picker remains (announcement may mislabel —
    heuristic until W4; record verbatim).
20. Die resting + Enter → action activates, outcome auto-reads.
21. T outside tutorial → mode-aware refusal (changed wording).

## Conversation
22. Auto-read; R rereads; Enter continues; response menu numbers + Up/Down;
    U mid-dialogue → "In conversation. Character window not available here."

## Cycle end
23. Quiet sweep → "Cycle complete" summary; V answers during transition.

## Cloud (optional)
24. S → scan mode; L → "In the cloud."; I/U/J → refusals; Backspace → refusal
    (deliberate, W4); S exits.
